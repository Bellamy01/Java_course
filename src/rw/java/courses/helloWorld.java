package rw.java.courses;

public class helloWorld{
        public static void main(String args[]) {
                System.out.print("Hello Rca students");
        }
        //package is a folder used to group related directories
        // -to avoid name conflicts
        // -to write maintainable code

        //there are two categories:
        //-built-in packages(packages from java API)
        //-user-defined packages(packages created by the user)
}
